<%= form_with(model: admin_welcome) do |form| %>
  <% if admin_welcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_welcome.errors.count, "error") %> prohibited this admin_welcome from being saved:</h2>

      <ul>
        <% admin_welcome.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :index %>
    <%= form.text_field :index %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
